# Documentation audit: agent and session architecture release

**Date:** 2026-09-02 09:05 UTC
**Commit:** `68acd29`
**Status:** PASS AFTER UPDATES

## Scope

Reviewed the active documentation for the agent descriptor, transcript format,
session store and runtime registries; the `cogpit-memory` search changes; the
2.1.0 app release; and npm publication of `cogpit` through the tag workflow.
Historical plans and design notes were not rewritten.

## Documentation changes and verification

- Corrected the architecture policy to distinguish the target agent boundary
  from the current vocabulary budget. `check:agents` counts named-agent lines
  and prevents growth; it does not analyze control flow.
- Documented the extra containment work needed when adding another CLI and
  clarified that the memory sync copies an explicit module list.
- Updated the architecture overview to use agent-specific adapter terminology.
- Corrected stale `cogpit-memory` examples that still described cross-session
  search as limited to Claude Code and Copilot. The index now covers all three
  supported CLIs.
- Documented that the release tag stamps `packages/cogpit-cli/package.json`
  before publishing `cogpit`, while `cogpit-memory` keeps its independent
  version.
- Verified the README configuration-browser description against the descriptor
  layouts and left public API fields that still use `provider` unchanged.

## Quality gates

The commit workflow reported these passing results: lint; production and test
typechecks; 346 Vitest files with 5,150 tests; coverage at 81.9% statements,
73.8% branches, 84.39% functions and 84.63% lines; dependency audit with zero
root findings and one accepted low finding in `cogpit-memory`; architecture
with 608 production files, 2,462 local edges and no cycles; agent vocabulary at
163 files and 999 lines; duplicate ratchets at 70 of 80 clones, 673 of 728 lines
and 0.68%; sync of 18 `cogpit-memory` modules; web and Electron builds; the
`cogpit` package contract; and 105 `cogpit-memory` tests plus its package build
and contract on Bun 1.3.14.

## Risk

MEDIUM. The change reorganizes session parsing, storage and live runtimes across
all supported CLIs. The release and npm paths remain the existing tag-driven
workflow. The documentation corrections do not change runtime or package
behavior.
