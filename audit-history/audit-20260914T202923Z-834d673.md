# Claude compatibility documentation audit

Date: 2026-09-14
Commit: 834d673
Verdict: PASS

The commit handles Claude staged file edits and structured Bash file diffs, updates Agent SDK to 0.3.270, preserves custom system prompts, and adds `/output-style` suggestions.

Updated `docs/tool-call-rendering.md` and `docs/usage.md` for awaiting-review labels and activity counts, proposed input and result visibility, applied-change and undo exclusions, Bash diff and omission notices, and the new command. Existing architecture documentation remains accurate. The September 14 compatibility audit records the release comparison, live probe limits, implementation and validation.

The code review found that identical or opposing Bash hunks could be collapsed and blank-line changes lost. The correction groups hunks per file, preserves line terminators and supplied counts, and adds regressions. Staged owner-review behavior was tested with fixtures, not a live owner-review session. Shell diffs remain display-only for undo, and omitted file hunks cannot be reconstructed.

Validation: 6,432 app tests across 410 files; all 108 memory tests on pinned Bun 1.3.14; production and test typechecks; ESLint; architecture, agent vocabulary, duplicate and generated-memory checks; production web build. React Doctor reported no errors and two existing timeline complexity warnings. The installed Bun canary reproduces its documented SQLite teardown crash. Earlier desktop/mobile component QA passed; final line-rendering changes have focused DOM regression tests.

The commit was assembled and tested in an isolated checkout. Other pending CLI, plugin, mobile and timeline styling changes remain outside it. The SDK custom-prompt helper and its test are included because they are required by the updated SDK type contract. No push or deployment was performed.
