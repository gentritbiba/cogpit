# Audit: Codex Patch Parsing and v1.0.1 Release (2026-07-20)

**Commit ID:** 37d4967
**Scope:** Release commit (5 tracked files; 704 insertions, 63 deletions)

## Implementation Summary

- Bumped the application package version from `1.0.0` to `1.0.1`.
- Extended Codex session parsing to recognize `tools.apply_patch(...)` calls nested inside modern `exec` wrappers while preserving the visible wrapper call.
- Added structured custom-tool output parsing and more precise error inference so successful patch edits are not marked failed by unrelated later command failures or summaries such as `0 failed`.
- Resolved relative patch paths against the session working directory and retained source/destination metadata for moved files.
- Reused the normalized Codex patch parser in `session-file-changes` so timeline tool cards and aggregated file changes agree.
- Added regression coverage for exec-wrapped patches, structured outputs, moves, path resolution, file-change aggregation, and sequential patch failure attribution.

## Documentation Reviewed

- `README.md`, especially the Live Sessions and File Changes feature descriptions
- `ARCHITECTURE.md`, including session parsing, the File Changes Panel, route organization, and server file-change tracking
- `package.json` release metadata
- Existing audit reports and `audit-history/audit-history.log`

## Documentation Impact

**Verdict: PASS — no product or architecture documentation update required.**

The change repairs ingestion of a newer Codex tool-call encoding for behavior Cogpit already documents: watching agents edit files and reviewing aggregated file changes. It does not add or remove an HTTP route, alter a request/response contract, introduce a configuration option, change a user workflow, or require a migration. The synthetic file-level tool calls remain internal parser data used to render the existing timeline and File Changes experience.

The `1.0.1` patch-version bump is correctly represented in `package.json`; the repository does not maintain a user-facing version table that requires a matching edit for every patch release.

## Files Reviewed

- `package.json`
- `src/lib/codex.ts`
- `src/lib/__tests__/codex.test.ts`
- `server/routes/session-file-changes.ts`
- `server/__tests__/routes/session-file-changes.test.ts`

No product documentation files were modified by this audit.
