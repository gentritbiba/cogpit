# Workspace rail and Bash command card audit

**Date:** 2026-09-03 12:34 UTC
**Commit:** d43337c
**Status:** PASS, drift corrected

## Scope

I reviewed the complete tracked diff, all three new source files, and the active product, plugin, and architecture documentation. The audit covered the desktop workspace rail, built-in and bundled plugin panels, panel state, the public plugin API, Worktrees, timeline Bash rendering, Codex exec rendering, and their tests.

## Behavior documented

- Worktrees now opens inside the right workspace rail instead of a separate sheet. Mission Control and Config also have direct activity-rail actions, and choosing a workspace panel returns the main view to Sessions.
- The panel context exposes worktree availability so the built-in Worktrees panel appears only where it is supported. Worktree data loads only while that panel is visible.
- GitHub Actions and Vercel Deployments share the new `FilterChip` controls. Their setup isolation and read-only CLI contracts are unchanged.
- Consecutive Bash calls render in one card, including calls split across adjacent tool-activity records. A non-Bash tool or thinking block starts a new group.
- Ordinary one-command Bash calls and labeled command batches now use the same compact, neutral presentation. Output, line counts, options, read/search links, and failure state remain attached to each command; red is reserved for failures.
- Codex exec script rendering moved into its own component without changing the tool contract.

## Documentation changes

- Updated `README.md` to describe consecutive Bash grouping, the shared single/batched command layout, and the Worktrees activity-rail entry point.
- Updated `docs/plugins.md` for the `supportsWorktrees` panel context field and the public `FilterChip` and `FilterChipCount` exports.
- `ARCHITECTURE.md` and `docs/architecture/README.md` remain accurate. The change stays inside the existing renderer orchestration and compile-time plugin boundaries, with no new route, persisted data, or runtime-plugin contract.
- No deployment registry update is needed because this change does not add or move a deployment, database, or service.

## Result

Active documentation now matches the implementation. No backend extension point, deployment mutation, or new external setup requirement is implied.

**Risk:** LOW. The changes affect renderer composition and presentation. Existing panel, capability, state, and timeline tests cover the new behavior.
