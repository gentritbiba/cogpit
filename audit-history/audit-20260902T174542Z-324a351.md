# Compile-time UI plugins and GitHub Actions panel audit (v2.2.1)

**Date:** 2026-09-02 17:45 UTC
**Commit:** 324a351
**Status:** PASS — drift corrected

## Summary

- Added a typed, build-time plugin registry and public renderer API for right-workspace panels, including ordering, visibility, sizing, badges, live indicators, and opt-in state preservation.
- Consolidated Project files, File changes, and Session details into one right activity rail where only one panel is visible at a time.
- Added the bundled `plugins/github-actions` panel. Its core-owned API route resolves the current repository and uses authenticated GitHub CLI to show workflow runs, jobs, steps, progress, and failures.
- Bumped the application to `2.2.1` and upgraded `@xmldom/xmldom`, `fast-uri`, and `qs` overrides to patched versions so the dependency audit ratchet remains green.

## Graceful GitHub CLI behavior

- A missing `gh` executable returns a typed `503 gh_missing` response; a signed-out CLI returns `gh_auth_required`.
- Setup errors remain contained to the GitHub Actions panel. Cogpit stays usable, the panel shows actionable guidance and manual retry, and automatic polling pauses until the user retries.
- Server, store, and panel regression tests cover the missing-CLI path.

## Documentation audit

- `README.md` documents the compile-time extension point and isolated `gh` setup state.
- `docs/plugins.md` documents registration, the public panel API, import boundaries, GitHub CLI setup, and the current lack of third-party backend route contributions.
- `docs/architecture/README.md` documents plugin dependency rules and canonical ownership.
- Deployment registries are intentionally updated only after the release workflow reports the published state.

## Verification

- Reviewer verdict: APPROVE after fixing project-root isolation and integrated `keepAlive` behavior.
- `bun run test`: 355 files, 5,235 tests passed.
- Lint, production and test type checks, architecture, dependency audit, duplication ratchet, generated-core sync, web build, and Electron build passed.

**Risk:** MEDIUM — new extension boundary and external CLI integration, mitigated by architecture enforcement, isolated failure states, and end-to-end regression coverage.
