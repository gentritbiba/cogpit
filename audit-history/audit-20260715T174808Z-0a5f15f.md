# Audit: Persisted Claude Mid-Turn Prompts (2026-07-15)

**Commit ID:** 0a5f15f
**Scope:** Claude Code prompts submitted while an agent turn is active

## Implementation Summary

- Added `queue-operation` to the Claude JSONL message model and parser guards.
- Preserved user-authored `enqueue` entries as chronological `queued_prompt` timeline blocks while filtering internal task and local-command notifications.
- Rendered persisted prompts as user cards labeled **Sent while Claude was working** and included their content in timeline search.
- Reconciled persisted queued prompts with optimistic pending-message previews so the UI does not duplicate a prompt after it reaches disk.
- Added parser, plan-mode grouping, scroll reconciliation, and search regression coverage.

## Documentation Impact

Reviewed `README.md`, `ARCHITECTURE.md`, and the documentation tree for session parsing, steering, timeline, and queued-message coverage.

**Verdict: PASS — no product documentation update required.**

The change repairs an internal persistence/display mismatch in behavior already described by the existing **Interactive Chat** (send or steer messages) and **Conversation Timeline** sections. It does not add a user workflow, public API route, configuration option, migration step, or externally consumed type. `ARCHITECTURE.md` already states that JSONL messages are aggregated into chronological `contentBlocks`; the new queue-operation variant is an implementation detail of that existing contract.

## Validation

- Focused regression suite: 169 tests passed
- TypeScript typecheck: passed
- Scoped lint for the intended files: passed
- Repository-wide lint: blocked by unrelated pre-existing issues outside this change

## Intended Change Set Reviewed

- `src/lib/types.ts`
- `src/lib/messageTypeGuards.ts`
- `src/lib/turnBuilder.ts`
- `src/components/timeline/TurnSection.tsx`
- `src/hooks/useChatScroll.ts`
- `src/lib/timelineHelpers.ts`
- `src/lib/__tests__/parser.test.ts`
- `src/lib/__tests__/turnBuilder.test.ts`
- `src/hooks/__tests__/useChatScroll.test.ts`
- `src/lib/__tests__/timelineHelpers.test.ts`

No product documentation files were modified by this audit.
