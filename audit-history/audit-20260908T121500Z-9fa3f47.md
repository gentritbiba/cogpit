# Browser automation enhancements: Windows support, windowed browsers, Cloudflare fixes, Codex questions

Status: PASS. Documentation audit complete; drift corrected.

## Scope

Release v2.6.2 bundles: (1) Windows browser-panel support via Node shim + cmd launcher, TCP port routing, process control updates; (2) named browsers opening in real Chrome/Chromium windows instead of headless, with automation flag disabled to pass Cloudflare verification (DisableAutomationControlled, AGENT_BROWSER_HEADED/EXECUTABLE_PATH env); (3) clipboard paste forwarding in Browser panel (paste event → Input.insertText, copy/cut roundtrip); (4) Codex question answering support (request_user_input → AskUserQuestion, async questions with receipt); (5) browser-activity detection for Codex code-mode exec calls; (6) interactiveState refactoring to detect async questions alongside blocking prompts; (7) version bump to 2.6.2.

## Files changed (docs only)

| File | Change |
|------|--------|
| `docs/browser.md` | Removed Windows limitation statement; changed "headless Chromium" to "Chromium"; added Windows subsection (Node shim, TCP ports, process control); added "Window or headless" subsection (browser detection, Cloudflare automation flag, display requirements, throwaway always-headless); added clipboard section (paste/copy forwarding); added troubleshooting entry for Cloudflare verification loops |
| `docs/cloudflare-browser-investigation-2026-09-08.md` | New file: investigation notes on navigator.webdriver flag, Turnstile rejection, windowed Chrome + `--disable-blink-features=AutomationControlled` solution, testing matrix, and "Not done" section on limitations |

## Drift detection

**README.md**: Mentions Windows as available platform and headless browsers in cleanup context. No platform-specific browser features documented (product-level only). Status: accurate, no changes needed.

**ARCHITECTURE.md**: Describes browser panel and notes shim ownership, types sync, and remote hub support. No Windows or headless specifics. Status: accurate, no changes needed.

**CLAUDE.md**: Browser Panel section notes routing shim, types sync, session routing, and remote device support. No Windows limitations or headless behavior documented. Status: accurate, no changes needed.

**docs/tool-call-rendering.md**: Lists `request_user_input`, `request_user_input_async`, and `AskUserQuestion` as display names for "Ask question". No changes to this table (all three names already listed, no distinction between blocking/async needed here). Status: accurate, no changes needed.

**server/browser/skill.ts**: Agent skill text. Does not mention Windows, headless, or Cloudflare. Guidance on named vs throwaway, subagent rules, browser flags, and handoffs remains accurate. Status: no changes needed (internal skill delivery, not subject to versioning drift).

## Files written

- None. All documentation drift was pre-corrected in the staged diff.

## Validation

- docs/browser.md: Windows subsection documents Node shim, TCP ports, and process management (taskkill) without contradicting POSIX shim behavior. "Window or headless" section correctly notes named browsers open in window (macOS/Linux when browser found, Linux when $DISPLAY set, Windows never), throwaway always headless, and automation flag off for Turnstile fix.
- docs/cloudflare-browser-investigation-2026-09-08.md: Investigation findings recorded; no forward-reference in browser.md (see "Window or headless" → investigation date 2026-09-08 implicit in section name, explicit in findings table).
- All refs to browser automation and Cloudflare now accurate to code behavior.

## Verdict

PASS — all documentation drift caused by this feature is corrected in the diff. No additional updates required. The changes are additive (Windows support newly enabled, named-browser windowing newly enabled) and the docs clearly distinguish when each applies.
