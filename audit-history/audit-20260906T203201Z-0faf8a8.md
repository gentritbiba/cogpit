# Browser routing audit

Date: 2026-09-06 UTC
Commit reviewed: 0faf8a8
Verdict: PASS after preserving effective configuration and correcting scope descriptions.

The reported session ran `agent-browser open` successfully, but its saved shell
snapshot put the ordinary executable ahead of Cogpit's shim. App-server thread
configuration now explicitly restores the managed PATH after shell snapshots.
It reads effective project configuration, preserves custom tool paths, and appends
the shared browser guide to existing developer instructions without a skill read.

The documentation distinguishes new threads and disk resume from rejoining an
already-loaded thread. The installed runtime retains configuration on loaded
resume; restarting Cogpit is required to retrofit existing threads. Legacy exec
and SDK-only subagent enforcement remain outside this change's scope.

Validation:

- Code review, simplification review, and documentation audit passed.
- Focused suites: 3 files, 103 tests passed.
- Full Vitest suite: 396 files, 6,192 tests passed.
- Lint, typecheck, agent vocabulary, architecture, and duplicate checks passed.
- Electron build passed.
- Actual installed runtime opened a local page through the changed execution path
  without reading a skill and described interaction in the Browser panel.
- Isolated browser API reported the default browser running; the panel socket
  reached `live` and delivered a 10,894-byte frame at 1280 by 720 pixels.
- Native loaded-resume probe retained the original session marker; disk resume
  after a runtime restart applied the new marker and resolved Cogpit's shim.

Runtime logs and probe scripts are retained locally at
`/tmp/cogpit-codex-browser-evidence/`. Test browsers and temporary profiles were
closed and removed; the user's browser and session were left running.
