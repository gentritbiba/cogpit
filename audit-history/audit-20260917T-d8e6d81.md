# Undo/redo/branching internal fixes audit

Date: 2026-09-17
Commit: d8e6d81
Verdict: PASS

The commit fixes four silent failures in undo, redo, and branching that had accrued since tail loading and file checkpoints shipped: file rewinds now occur before transcript truncation (so checkpoints are found), the transcript is measured before preflight (avoiding random undo rejections), branches carry `branchPointTurnId` and are re-resolved on every read (fixing vanishing redo and drift on reload), and turn cuts match parsed turn IDs for all agents (fixing Codex cuts that kept wrong turns). `UndoState.currentTurnIndex` and `totalTurns` fields removed as never read.

Documentation was checked against the diff. No API route shape changed: `/api/branch-session` still accepts `turnUuid` in the request body and returns the same response. User-facing behavior is unchanged (branching, undo, and redo work the same way externally). ARCHITECTURE.md, README.md, docs/usage.md, docs/architecture/README.md, .claude/skills/cogpit-sessions/SKILL.md, packages/cogpit-memory/skill/SKILL.md, ios/API_MAP.md and ios/DESKTOP_FLOWS.md all remain accurate; no implementation detail exposure in these docs.

Verification: 478 Vitest files and 7873 tests pass, all nine cogpit-memory test files pass, and lint, typecheck, architecture, agent-vocabulary and cogpit-memory sync checks pass. Undo, redo, branch switch, nested branches and duplicate were exercised live against Claude, Codex and Copilot sessions on a standalone build before the review-stage cleanups, which are covered by tests only.
