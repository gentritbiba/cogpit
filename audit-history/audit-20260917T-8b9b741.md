# Runtime plugins, Cloudflare Workers panel and plugin signing audit

Date: 2026-09-17
Commit: 8b9b741
Verdict: PASS

The commit lands the runtime plugin system that had been developing in the working tree: sandboxed iframe packages with a `plugin.json` manifest and IIFE entry, the host installer and TUF-verified store under `server/plugins/`, the public `@cogpit/plugin-contracts`, `plugin-sdk`, `plugin-ui` and `plugin-integrations` workspaces, and the ClickUp, GitHub and Vercel panels rebuilt as installable packages with one-time migration for existing hosts. On top of that it adds the Cloudflare Workers plugin (host Wrangler adapter with nearest-config discovery, child-project scan and Worker picker, per-environment configuration, deployments and version details, plus an Account tab over an API-token HTTPS connection) and `@cogpit/plugin-tools`, whose `cogpit-plugin` CLI signs built directories into `.cogpit-plugin` files that install through Publishers and Install from file without rebuilding Cogpit.

Documentation was checked against the diff. `docs/plugins.md`, `docs/runtime-plugin-recovery.md`, `docs/runtime-plugin-browser-support.md`, `docs/development.md`, `docs/browser.md`, `README.md` and the package READMEs were already updated in the diff. `docs/usage.md` still said runtime plugin installation was unsupported and omitted Cloudflare; it now lists the four integrations, Wrangler 4.65.0 or newer, and file-based installation. `ARCHITECTURE.md` needed no change: the agent layer and route registry are untouched.

Verification recorded for this commit: 476 Vitest files and 7854 tests pass; lint, production and test typechecks, the architecture and agent-vocabulary checks and the external package-contract fixture pass. Real browser QA on an isolated standalone host covered Browse installation, file installation of a development-signed package, the Worker picker on a repository with two child Workers, environment switching and version details against live Cloudflare data. Not verified: the Account tab's live listing (no API token available), Windows execution, and the oldest supported browsers. The public packages remain unpublished 1.0.0 workspaces and the online catalog is deferred.
