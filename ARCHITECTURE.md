# Cogpit architecture

The maintained architecture guide lives in
[`docs/architecture/README.md`](docs/architecture/README.md).

That guide defines the dependency rules, canonical ownership map, required
quality gates, and change strategy for the renderer, server, Electron host,
shared session core, and published `cogpit-memory` package.
