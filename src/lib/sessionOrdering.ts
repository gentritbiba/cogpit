/** @deprecated Import session ordering utilities from `shared/session-ordering` in new code. */
export {
  compareSessionsByRecency,
  getSessionRecencyMs,
  sortSessionsByRecency,
  type SessionRecencyLike,
} from "../../shared/session-ordering"
